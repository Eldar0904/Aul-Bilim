/*
  Copy this file to uploads/firebase-config.js and fill it with your Firebase
  web app values. Do not commit real secrets or service-account credentials.

  The public API key is safe to ship for Firebase web apps when Firestore and
  Storage security rules enforce authenticated admin writes.
*/
window.AUL_BILIM_FIREBASE_CONFIG = {
  apiKey: "YOUR_FIREBASE_WEB_API_KEY",
  projectId: "YOUR_FIREBASE_PROJECT_ID",
  storageBucket: "YOUR_FIREBASE_STORAGE_BUCKET.appspot.com",
  authDomain: "YOUR_FIREBASE_PROJECT_ID.firebaseapp.com"
};
